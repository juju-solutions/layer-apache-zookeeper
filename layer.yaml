repo: git@github.com:juju-solutions/layer-apache-zookeeper.git
includes: ['layer:basic', 'interface:zookeeper-quorum', 'interface:zookeeper']
options:
  basic:
    packages:
      - unzip
      - ant
      - default-jdk
